# EchoWrite 快速上手與語音指令指南 (Quick Start & Voice Commands)

歡迎使用 **EchoWrite**！本指南將帶您在 30 秒內掌握雙平台與桌面端的核心操作。

---

## ⚡ 30 秒快速上手 (Quick Start)

### 1. 桌面端 (macOS / Windows)
- **開始 / 完成錄音**：
  - **macOS**：按快捷鍵 <kbd>Command</kbd> + <kbd>Shift</kbd> + <kbd>E</kbd> 或點擊選單列圖示。
  - **Windows**：按快捷鍵 <kbd>Alt</kbd> + <kbd>S</kbd> 或點擊右下角托盤圖示。
- **自動打字**：說完話後，系統會自動在您當前焦點的游標處（VS Code、Word、LINE、瀏覽器等）替換為排版完美的繁體中文。
- **取消錄音**：點擊靈動島上的 `✕` 按鈕或按 <kbd>Esc</kbd> 即可捨棄本次錄音。

### 2. 行動端 (iOS / Android 鍵盤)
- **錄音與樂觀串流**：點擊中間發光的「麥克風」按鈕開始說話，螢幕會即時顯示語音草稿。
- **滑動取消**：錄音中若想作廢，**向左滑動超過按鈕**即可立即捨棄並清除草稿。
- **風格切換**：鍵盤頂部膠囊列可左右滑動，一鍵切換 5 種口吻風格。

---

## 🗣️ 語音編輯指令速查 (Voice Commands Cheatsheet)

在說話過程中，您可以直接說出以下指令，系統會在 5ms 內以微秒級 fast-path 完成動作：

| 語音說法 | 執行效果 |
| :--- | :--- |
| **「換行」** 或 **「下一行」** | 插入一個換行符號 `\n` |
| **「空兩行」** 或 **「換兩行」** | 插入兩個換行符號 `\n\n` |
| **「加個問號」** 或 **「問號」** | 插入全形問號 `？` |
| **「驚嘆號」** 或 **「感嘆號」** | 插入全形驚嘆號 `！` |
| **「逗號」** / **「句號」** | 插入全形標點 `，` / `。` |
| **「冒號」** / **「分號」** | 插入全形標點 `：` / `；` |

---

## 🎭 5 大語意風格介紹 (5 AI Tone Styles)

1. **⚡ 極簡口語 (`casual`)**：去除「呃、那個、然後」等口頭贅字，保留口語自然語調與正確標點。
2. **🏛️ 專業公文 (`formal`)**：轉換為行政敬語、書面正式報告與公務用語。
3. **✉️ 商務 Email (`email`)**：自動梳理為標準信件格式（問候語、正文結構、結尾祝詞）。
4. **🌐 中英雙語 (`bilingual`)**：上方為繁中排版，下方自動附上地道專業英文對照。
5. **📋 條列重點 (`bullet`)**：將長篇口述自動提煉為 Markdown 重點清單。

---

## 🇹🇼 台灣在地化與中英混說特點
- **中英夾雜自動大寫與空格**：自動修正 `PR`、`API`、`UI`、`App`、`SDK`、`CI/CD` 等科技用語。
- **台語常用語正字映射**：自動將口語「真拍謝」轉為「歹勢」、「代誌大條」、「凍未條」。
